---
phase: 03-practica-y-progreso
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/[locale]/progress/page.tsx
  - src/components/exercises/exercise-progress.tsx
  - src/lib/exercises/get-progress.ts
  - messages/es.json
  - messages/en.json
autonomous: true
requirements:
  - PROG-02
  - PROG-03
must_haves:
  truths:
    - "Student can view progress of figures they've practiced"
    - "Student can see completed exercises count"
    - "Student can see their saved writing submissions"
  artifacts:
    - path: "src/app/[locale]/progress/page.tsx"
      provides: "Progress dashboard"
      contains: "stats, per-figure grid, saved writings"
---

<objective>
Build the progress dashboard page where students can see their completed exercises, accuracy, figure mastery, and saved writing samples.

Purpose: Give students visibility into their learning journey.
Output: Responsive progress page with summary stats, per-figure grid, and saved writings list.
</objective>

<context>
- user_progress table exists with userId, figureId, exercisesCompleted, exercisesCorrect, lastPracticedAt
- writing_submissions table exists with userId, rawText, title, savedAt
- figures table exists with name, slug, difficultyLevel, category
- ExerciseProgress component already renders per-figure completion bars
- track-progress.ts already increments counters on exercise completion
- `src/app/[locale]/exercises/page.tsx` shows the page pattern to follow
</context>

<tasks>

<task type="auto">
  <name>Build progress data fetching and dashboard page</name>
  <files>
    src/lib/exercises/get-progress.ts,
    src/components/exercises/exercise-progress.tsx,
    src/app/[locale]/progress/page.tsx,
    messages/es.json,
    messages/en.json
  </read_first>
  <action>
    1. Create `src/lib/exercises/get-progress.ts`:
       - getUserProgress(userId): fetch all user_progress rows joined with figure name/slug/category/difficulty
       - getSavedWritings(userId): fetch writing_submissions ordered by savedAt desc
       - compute accuracy rate per figure and overall
    
    2. Update `src/components/exercises/exercise-progress.tsx`:
       - Add overall stats section (total exercises, accuracy %, figures practiced, days active)
       - Add per-figure grid with status badge, completion count, accuracy indicator, last practiced date
       - Add saved writings section showing title, preview, date
    
    3. Create `src/app/[locale]/progress/page.tsx`:
       - Server-rendered page with auth guard (redirect to login if not authenticated)
       - Fetches progress data and saved writings from the database
       - Renders the enhanced ExerciseProgress component
       - Responsive layout matching app patterns
    
    4. Add locale strings:
       - "Tu progreso" / "Your Progress"
       - Stats labels (ejercicios, figuras, precisión, textos guardados)
       - Status labels (completado, en progreso, sin empezar)
       - Date formatting
  </action>
</task>

</tasks>

<verification>
- `test -f src/app/[locale]/progress/page.tsx`
- `test -f src/lib/exercises/get-progress.ts`
- `grep -q "getUserProgress" src/lib/exercises/get-progress.ts`
- `npm test` and `npm run build` pass
</verification>

<success_criteria>
- Student can view progress per figure (exercises completed, accuracy)
- Student can see overall summary stats
- Student can see their saved writing submissions
</success_criteria>

<output>
After completion, create `.planning/phases/03-practica-y-progreso/03-02-SUMMARY.md`
</output>
