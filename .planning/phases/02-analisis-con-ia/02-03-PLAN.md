---
phase: 02-analisis-con-ia
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/db/schema.ts
  - drizzle/0001_phase2_writing.sql
  - src/lib/ai/cache.ts
  - src/lib/writing/submissions.ts
  - src/app/api/ai/save/route.ts
  - src/lib/ai/trace.ts
  - promptfoo.yml
  - src/lib/ai/analyze-text.test.ts
  - src/lib/ai/cache.test.ts
  - src/app/api/ai/save/route.test.ts
autonomous: true
requirements:
  - PROG-01
must_haves:
  truths:
    - "Student can explicitly save a writing sample to their account"
    - "Saved writings are linked to the authenticated user and persist in the database"
    - "Repeated analysis can reuse cached results keyed by text hash and locale"
    - "Prompt regression tests exist for the AI tutor"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Phase 2 writing persistence schema"
      contains: "writingSubmissions, analysisRuns"
    - path: "src/app/api/ai/save/route.ts"
      provides: "Explicit save endpoint"
      contains: "saved writing payload"
    - path: "src/lib/ai/cache.ts"
      provides: "Analysis cache helper"
      contains: "content hash and locale keying"
    - path: "promptfoo.yml"
      provides: "AI regression test config"
      contains: "20-example dataset"
  key_links:
    - from: "src/lib/ai/cache.ts"
      to: "src/lib/ai/analyze-text.ts"
      via: "cache lookup"
      pattern: "contentHash"
    - from: "src/app/api/ai/save/route.ts"
      to: "src/db/schema.ts"
      via: "write persistence"
      pattern: "writingSubmissions"
---

<objective>
Persist student writing samples explicitly, cache repeat analyses, and add the first evaluation harness for the AI tutor.

Purpose: Complete the Phase 2 progression requirement and establish the quality loop that will keep the tutor reliable as content grows.
Output: DB schema + save route + cache service + regression evals.
</objective>

<execution_context>
@/Users/nestor/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/nestor/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-analisis-con-ia/02-CONTEXT.md
@.planning/phases/02-analisis-con-ia/02-AI-SPEC.md
@.planning/phases/02-analisis-con-ia/02-01-SUMMARY.md
@.planning/phases/02-analisis-con-ia/02-02-SUMMARY.md
@.planning/phases/01-fundamentos-y-catalogo/01-02-SUMMARY.md

**Key decisions from CONTEXT.md:**
- Saving text is explicit, not automatic
- Cached analysis should be keyed by content hash + locale + catalog version
- The AI tutor must be continuously evaluated with a labeled reference set

**Evaluation contract:**
- Start with 20 examples
- Use Promptfoo for regression tests
- Include failure modes for ambiguity, tone drift, and regional mismatch
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add persistence schema for writing samples and analysis runs</name>
  <files>src/db/schema.ts, drizzle/0001_phase2_writing.sql</files>
  <read_first>
    - src/db/schema.ts (Phase 1 schema)
    - drizzle/0000_white_sleeper.sql (existing baseline migration)
    - .planning/phases/02-analisis-con-ia/02-AI-SPEC.md (persistence expectations)
  </read_first>
  <action>
    1. Extend `src/db/schema.ts` with two new tables:
       - `writingSubmissions`: id, userId, locale, title, rawText, savedAt, createdAt, updatedAt
       - `analysisRuns`: id, submissionId, model, locale, contentHash, confidence, analysisJson, feedbackJson, createdAt
    2. Add relations so a submission has many analysis runs and a user has many submissions.
    3. Generate the corresponding migration SQL in `drizzle/0001_phase2_writing.sql` with proper foreign keys and cascade behavior.
    4. Keep the schema compatible with Supabase Postgres and the existing Drizzle client.
  </action>
  <verify>
    <automated>test -f src/db/schema.ts && test -f drizzle/0001_phase2_writing.sql && grep -q "writingSubmissions" src/db/schema.ts && grep -q "analysisRuns" src/db/schema.ts</automated>
  </verify>
  <done>The Phase 2 persistence schema exists and can store saved student writing plus cached analysis runs</done>
</task>

<task type="auto">
  <name>Task 2: Implement explicit save flow and analysis cache</name>
  <files>src/lib/ai/cache.ts, src/lib/writing/submissions.ts, src/app/api/ai/save/route.ts, src/lib/ai/trace.ts</files>
  <read_first>
    - src/db/schema.ts
    - src/lib/ai/analyze-text.ts
    - src/lib/ai/schemas.ts
    - src/lib/auth/server.ts
  </read_first>
  <action>
    1. Create `src/lib/ai/cache.ts` with a deterministic cache key based on `contentHash`, `locale`, and `catalogVersion`.
    2. Create `src/lib/writing/submissions.ts` with helpers to save, load, and update writing submissions for the authenticated user.
    3. Create `src/app/api/ai/save/route.ts` that accepts an explicit save request and persists the current writing sample only when the user clicks save.
    4. Create `src/lib/ai/trace.ts` with a lightweight tracing adapter so analysis requests can be sampled in production monitoring.
    5. Ensure cached analyses are reused when the same user submits the same text and locale again.
  </action>
  <verify>
    <automated>test -f src/lib/ai/cache.ts && test -f src/lib/writing/submissions.ts && test -f src/app/api/ai/save/route.ts && grep -q "contentHash" src/lib/ai/cache.ts</automated>
  </verify>
  <done>Students can explicitly save writing samples and repeat analyses can be served from cache when the input has not changed</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests and Promptfoo evals</name>
  <files>promptfoo.yml, src/lib/ai/analyze-text.test.ts, src/lib/ai/cache.test.ts, src/app/api/ai/save/route.test.ts</files>
  <read_first>
    - src/lib/ai/analyze-text.ts
    - src/lib/ai/cache.ts
    - src/app/api/ai/save/route.ts
    - vitest.config.ts
  </read_first>
  <action>
    1. Create a `promptfoo.yml` regression suite with 20 labeled examples covering clear figures, ambiguous cases, regional examples, and tutor-tone failures.
    2. Add Vitest tests for the cache key logic and save route authorization.
    3. Add at least one test that confirms identical text + locale reuses the same cache key.
    4. Keep the tests deterministic and free of live model calls.
  </action>
  <verify>
    <automated>test -f promptfoo.yml && test -f src/lib/ai/cache.test.ts && test -f src/app/api/ai/save/route.test.ts && grep -q "examples" promptfoo.yml</automated>
  </verify>
  <done>The tutor now has persistence and a regression eval harness for saved texts, cache behavior, and output quality</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User action → saved submission | Text is saved only when explicitly requested |
| Cached analysis → reused response | Cache key must prevent cross-user leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Information Disclosure | Saved writing samples | mitigate | Tie every submission to the authenticated user |
| T-02-21 | Tampering | Cache entries | mitigate | Hash text + locale + catalog version |
| T-02-22 | Repudiation | Save action | accept | Record timestamps and user IDs |
</threat_model>

<verification>
- `test -f src/db/schema.ts && test -f drizzle/0001_phase2_writing.sql`
- `test -f src/lib/ai/cache.ts && test -f src/lib/writing/submissions.ts && test -f src/app/api/ai/save/route.ts`
- `test -f promptfoo.yml`
- `grep -q "writingSubmissions" src/db/schema.ts`
- `grep -q "analysisRuns" src/db/schema.ts`
- `npm test` passes after save/cache tests are added
</verification>

<success_criteria>
- Student can explicitly save a writing sample to their account
- Saved writings are linked to the authenticated user and persist in the database
- Repeated analysis can reuse cached results keyed by text hash and locale
- Prompt regression tests exist for the AI tutor
</success_criteria>

<output>
After completion, create `.planning/phases/02-analisis-con-ia/02-03-SUMMARY.md`
</output>
