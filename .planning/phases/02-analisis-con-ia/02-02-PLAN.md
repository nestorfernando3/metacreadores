---
phase: 02-analisis-con-ia
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - package.json
  - src/app/[locale]/write/page.tsx
  - src/components/writing/writing-editor.tsx
  - src/components/writing/writing-toolbar.tsx
  - src/components/writing/feedback-panel.tsx
  - src/components/writing/analysis-highlights.tsx
  - src/components/writing/analysis-result-card.tsx
  - messages/es.json
  - messages/en.json
autonomous: true
requirements:
  - WRITE-01
  - WRITE-02
must_haves:
  truths:
    - "Student can write text in a TipTap editor"
    - "AI matches are rendered as inline highlights using exact offsets"
    - "Tutor feedback is visible beside the editor and updates after analysis"
    - "The writing experience works on mobile and desktop"
  artifacts:
    - path: "src/components/writing/writing-editor.tsx"
      provides: "TipTap writing surface"
      contains: "EditorContent"
    - path: "src/components/writing/analysis-highlights.tsx"
      provides: "Inline highlight renderer"
      contains: "highlight ranges"
    - path: "src/components/writing/feedback-panel.tsx"
      provides: "Tutor feedback panel"
      contains: "summary, strengths, suggestions"
    - path: "src/app/[locale]/write/page.tsx"
      provides: "Writing page"
      contains: "WritingEditor"
  key_links:
    - from: "src/app/[locale]/write/page.tsx"
      to: "src/components/writing/writing-editor.tsx"
      via: "import"
      pattern: "WritingEditor"
    - from: "src/components/writing/writing-editor.tsx"
      to: "src/components/writing/analysis-highlights.tsx"
      via: "highlight extension"
      pattern: "analysis-highlights"
---

<objective>
Build the student writing experience with a TipTap editor, inline figure highlighting, and a tutor feedback panel that presents the AI analysis from Phase 2.

Purpose: Turn the analysis engine into a usable learning loop where students can see rhetorical figures inside their own writing.
Output: Responsive writing page with editor, highlights, and feedback UI.
</objective>

<execution_context>
@/Users/nestor/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/nestor/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-analisis-con-ia/02-CONTEXT.md
@.planning/phases/02-analisis-con-ia/02-AI-SPEC.md
@.planning/phases/02-analisis-con-ia/02-01-SUMMARY.md
@.planning/phases/01-fundamentos-y-catalogo/01-03-SUMMARY.md

**Key decisions from CONTEXT.md:**
- Student writes or pastes text, then the AI highlights figures inline and explains them in tutor tone
- The page must work on both mobile and desktop
- The analysis response comes from the AI route created in Plan 01

**UI contract:**
- TipTap is the editor foundation
- Inline highlights must reflect exact offsets from the analysis result
- Feedback panel should summarize strengths, suggestions, and cautionary notes
- The writing page must remain friendly and calm, not graded or score-heavy
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TipTap editor dependencies and base editor component</name>
  <files>package.json, src/components/writing/writing-editor.tsx, src/components/writing/writing-toolbar.tsx</files>
  <read_first>
    - package.json (current app dependencies)
    - src/app/[locale]/write/page.tsx (if it exists after scaffolding)
    - .planning/phases/02-analisis-con-ia/02-AI-SPEC.md (editor interaction requirements)
  </read_first>
  <action>
    1. Install TipTap packages:
       ```bash
       npm install @tiptap/react @tiptap/starter-kit @tiptap/extension-highlight @tiptap/extension-placeholder
       ```
    2. Create `src/components/writing/writing-editor.tsx` as a client component that:
       - initializes TipTap with StarterKit + Placeholder + Highlight
       - accepts `value`, `onChange`, `onAnalyze`, and `highlights` props
       - renders a calm, responsive editor surface with a toolbar
    3. Create `src/components/writing/writing-toolbar.tsx` with the minimal controls needed for this phase (bold, italic, bullet list, analyze button).
    4. Keep the editor reusable so the writing page can pass AI highlight ranges from the analysis response.
  </action>
  <verify>
    <automated>test -f src/components/writing/writing-editor.tsx && test -f src/components/writing/writing-toolbar.tsx && grep -q "useEditor" src/components/writing/writing-editor.tsx && grep -q "@tiptap" package.json</automated>
  </verify>
  <done>The TipTap editor foundation exists and exposes the hooks needed for AI analysis highlights</done>
</task>

<task type="auto">
  <name>Task 2: Build the writing page, highlight rendering, and feedback panel</name>
  <files>src/app/[locale]/write/page.tsx, src/components/writing/analysis-highlights.tsx, src/components/writing/feedback-panel.tsx, src/components/writing/analysis-result-card.tsx</files>
  <read_first>
    - src/components/writing/writing-editor.tsx
    - src/lib/ai/schemas.ts
    - src/lib/ai/analyze-text.ts
    - messages/es.json
    - messages/en.json
  </read_first>
  <action>
    1. Create `src/components/writing/analysis-highlights.tsx` to map exact text offsets from the AI result into TipTap highlight decorations.
    2. Create `src/components/writing/feedback-panel.tsx` to render the tutor summary, strengths, suggestions, and caution note.
    3. Create `src/components/writing/analysis-result-card.tsx` for individual figure matches with figure name, confidence, explanation, and optional alternative.
    4. Build `src/app/[locale]/write/page.tsx` as a responsive writing workspace that shows the editor on the left/top and the feedback panel on the right/bottom.
    5. Make the page locale-aware and keep the copy aligned with the tutor tone rules.
  </action>
  <verify>
    <automated>test -f src/app/[locale]/write/page.tsx && test -f src/components/writing/feedback-panel.tsx && test -f src/components/writing/analysis-highlights.tsx && grep -q "feedback" src/components/writing/feedback-panel.tsx</automated>
  </verify>
  <done>The student writing page renders the editor, inline highlights, and the tutor feedback panel in a responsive layout</done>
</task>

<task type="auto">
  <name>Task 3: Add locale strings and UI state handling</name>
  <files>messages/es.json, messages/en.json</files>
  <read_first>
    - messages/es.json
    - messages/en.json
    - src/app/[locale]/write/page.tsx
  </read_first>
  <action>
    1. Add localized strings for the writing page, analyze button, loading state, empty state, error state, and feedback labels.
    2. Keep Spanish as the default voice and English as a faithful translation, not a literal machine translation.
    3. Ensure the page has clear microcopy for retrying analysis and saving work later in Phase 3.
  </action>
  <verify>
    <automated>grep -q "write" messages/es.json && grep -q "write" messages/en.json && grep -q "analyze" messages/es.json && grep -q "analyze" messages/en.json</automated>
  </verify>
  <done>The writing experience has localized copy and the UI states needed for the analysis loop</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor content → AI analysis | Student writing is sent for analysis only after explicit action |
| AI highlight output → editor decorations | Highlight ranges must be validated before rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Information Disclosure | Draft student writing | mitigate | Don’t auto-save drafts in Phase 2 |
| T-02-11 | Tampering | Highlight overlay | mitigate | Validate spans before decoration rendering |
| T-02-12 | Denial of Service | Repeated analyze clicks | mitigate | Disable the button while analysis is in flight |
</threat_model>

<verification>
- `test -f src/components/writing/writing-editor.tsx`
- `test -f src/components/writing/feedback-panel.tsx`
- `test -f src/components/writing/analysis-highlights.tsx`
- `test -f src/app/[locale]/write/page.tsx`
- `grep -q "@tiptap/react" package.json`
- `grep -q "writing" messages/es.json`
- `npm run build` completes after the editor page is wired up
</verification>

<success_criteria>
- Student can write text in a TipTap editor
- AI matches render as inline highlights using exact offsets
- Tutor feedback is visible beside the editor and updates after analysis
- The writing page works on mobile and desktop
</success_criteria>

<output>
After completion, create `.planning/phases/02-analisis-con-ia/02-02-SUMMARY.md`
</output>
